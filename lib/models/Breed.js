const mongoose = require('mongoose');

const breedSchema = new mongoose.Schema({
    breed: {
        type: String,
        required: [true, 'breed is required.']
    }
    



});



breedname
weight range 
